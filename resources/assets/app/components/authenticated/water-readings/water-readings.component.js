import template from "./water-readings.html";
import controller from "./water-readings.controller";

let WaterReadingsComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default WaterReadingsComponent;