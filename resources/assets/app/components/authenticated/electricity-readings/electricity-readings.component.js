import template from "./electricity-readings.html";
import controller from "./electricity-readings.controller";

let ElectricityReadingsComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default ElectricityReadingsComponent;