import template from "./gas-readings.html";
import controller from "./gas-readings.controller";

let GasReadingsComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default GasReadingsComponent;