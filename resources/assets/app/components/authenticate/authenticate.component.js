import template from "./authenticate.html";
import controller from "./authenticate.controller";

let AuthenticateComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default AuthenticateComponent;